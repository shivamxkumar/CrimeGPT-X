// Central Recharts palette/theme — single source so Dashboard + Analytics
// charts stay visually consistent instead of scattering hex literals per page.
export const chartColors = ['#3b82f6', '#8b5cf6', '#22c55e', '#f59e0b', '#60a5fa', '#ef4444', '#9ca3af', '#facc15', '#ec4899', '#14b8a6']

export const chartAxis = { fontSize: 11, fill: '#6b7280' }

export const chartTooltipStyle = {
  background: '#161d2c',
  border: '1px solid rgba(255,255,255,0.08)',
  borderRadius: 12,
  fontSize: 12,
  color: '#f3f4f6',
  boxShadow: '0 12px 32px -12px rgba(0,0,0,0.55)',
}

export const chartGrid = 'rgba(255,255,255,0.06)'
